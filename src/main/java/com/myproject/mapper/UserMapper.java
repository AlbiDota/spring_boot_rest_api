/*
import com.myproject.dto.UserBasicDTO;
import com.myproject.dto.UserDetailDTO;
import com.myproject.model.User;
import org.mapstruct.Mapper;
import org.mapstruct.NullValuePropertyMappingStrategy;

import java.util.List;

@Mapper(componentModel = "spring",
        nullValuePropertyMappingStrategy = NullValuePropertyMappingStrategy.IGNORE)
public interface UserMapper {
    UserBasicDTO toProductBasicDTO(User user);
    UserDetailDTO toUserDetailDTO(User user);

    List<UserBasicDTO> toUserBasicDTOs(List<User> user);
    List<UserDetailDTO> toUserDetailDTOs(List<User> user);

    // Add your custom mapping methods here
}
*/